/**
 * The API listener is used for distributed monitoring setups.
 */
object ApiListener "api" {

  accept_config = true
  accept_commands = true

{% if is_client %}
  bind_host = "{{ icinga_client_ip }}"

{% endif %}
}
